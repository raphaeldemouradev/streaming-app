import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    ContainerNavbar: {
        marginTop: 30,
        backgroundColor: '#000000ff',
        alignItems: 'center',
        padding: 20,
    },
    TextNavbar: {
        color: '#fff',
        fontSize: 20,
        fontWeight: 'bold',
    }
})